server.port=8080
spring.application.name=VoiceChanger
server.servlet.context-path=/

# File upload settings
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Logging
logging.level.com.example.voicechanger=ERROR
logging.level.com.example.voicechanger.service.esl=DEBUG
logging.level.root=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

# FreeSWITCH ESL Configuration
freeswitch.esl.host=127.0.0.1
freeswitch.esl.port=8021
freeswitch.esl.password=ClueCon
freeswitch.esl.timeout=10000
freeswitch.esl.reconnect.interval=5
freeswitch.esl.reconnect.max.attempts=10

# FreeSWITCH User Configuration
freeswitch.config.directory=/usr/local/freeswitch/conf/directory/default
freeswitch.user.password=humayun200044

# Application settings
voice.changer.max.buffer.size=8192
voice.changer.sample.rate=44100

# JVM settings for native library
java.library.path=src/main/resources/native

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/magic_call?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=tbuser
spring.datasource.password=Takay1takaane$
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
# ddl-auto=update: Automatically creates missing tables and adds new columns
# Does not drop existing tables or data
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Database Initialization
# Enable SQL script execution
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
# Defer initialization until after Hibernate creates tables
spring.jpa.defer-datasource-initialization=true

# JWT Configuration
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
jwt.expiration=86400000